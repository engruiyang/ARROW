# Samples

Place small local test images here when experimenting with ARROW.

Do not commit large images, datasets, videos, or binary sample packs. TASK 1A only
uses temporary empty image files in tests and does not read image pixels.
